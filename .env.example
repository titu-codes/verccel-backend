# Database - use one of the options below

# Option 1: MySQL (e.g. Aiven, Railway MySQL)
# DATABASE_URL=mysql+pymysql://user:password@host:port/database

# Option 2: SQLite for local dev (set USE_SQLITE=true or leave DATABASE_URL empty)
USE_SQLITE=true

# Optional
# JWT_SECRET=your_secret_key
